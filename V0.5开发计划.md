# BrowserPilot V0.5 开发计划

> **版本主题：** Memory —— 历史操作与上下文记忆
> **状态：** ✅ 已完成
> **前置版本：** V0.4 Reflection（错误恢复与重试）
> **目标：** 让 Agent 在长任务中不再丢失早期上下文 —— 超出滑动窗口的旧历史被压缩为摘要式记忆，新近步骤保留细节，LLM 始终拿到「摘要 + 最近窗口」的紧凑上下文。

---

## 1. 版本定位

V0.3 起 `serialize_history()` 只保留最近 5 条历史（滑动窗口），超出窗口的上下文被直接丢弃。长任务（登录 → 查询 → 下载 → 上传）超过窗口后，LLM 会忘记"已经做过什么"，导致重复输入、重复点击、重复导航。

V0.5 的核心是给 Agent 增加**增量式历史记忆**：

```text
Agent 每步执行
    ↓ 记录
HistoryMemory
    ├─ 原始条目（完整保留，Agent.history 不变）
    ├─ 摘要记忆（超出窗口的旧条目按批折叠，规则式默认 / 可注入 LLM 摘要器）
    └─ 滑动窗口（最近 N 条原始详情）
    ↓
context_entries() = [摘要?] + [最近窗口]
    ↓
Planner → serialize_history → LLM 上下文
```

### 1.1 交付目标

V0.5 完成后应具备：

1. `HistoryMemory`：增量式历史记忆对象 —— 条目只摘要一次，摘要逐批追加，不重复计算。
2. `summarize_entries()`：规则式摘要纯函数 —— 每条历史压缩为一行「动作 目标 → 结果」，确定性、零成本、可测试。
3. 上下文压缩 —— Planner 收到的上下文 = 摘要（旧） + 最近窗口（新），摘要不占窗口名额。
4. Agent 接入 —— 每步记录进记忆，向 `plan_with_history` / `plan_step` / `reflect` 传入压缩后上下文。
5. 可选 LLM 摘要器 —— 通过构造参数注入异步摘要函数，替换默认规则式摘要（扩展点，不影响默认行为）。

### 1.2 前置条件

> ✅ V0.4 已完成：Reflection 重试、页面恢复、停滞检测、两阶段任务队列。

### 1.3 本版本不包含

- 不做跨任务持久化记忆（文件/数据库存储，留待 V1.0 或后续）。
- 不做基于记忆的"行为偏好学习"（记用户习惯）。
- 摘要文本中不包含输入值、URL、截图等敏感或冗余内容（与 `serialize_history` 白名单一致）。

---

## 2. 架构设计

### 2.1 新增 / 修改模块

```text
agent/
├─ core/
│  ├─ memory.py        # 新增：HistoryMemory + summarize_entries（规则式摘要）
│  └─ agent.py         # 修改：持有 HistoryMemory，记录 + 传压缩上下文
├─ prompts/
│  └─ planner.py       # 修改：serialize_history / build_user_prompt 支持摘要条目
└─ tests/
   └─ test_memory.py   # 新增：摘要 + 折叠 + 上下文压缩 + Agent 集成
```

依赖方向不变（单向）：

```text
core/memory ──→ schema（纯数据，无外部依赖）
prompts ──→ schema
core/agent ──→ core/memory + core/planner
```

`core/memory.py` 不依赖 LLM / Playwright；LLM 摘要器由调用方以 async callable 注入。

### 2.2 数据流

```text
Agent 每步: entry = {"step", "action", "observation"}
    → self._history.append(entry)          # 原始历史（公开 API 不变）
    → self._memory.add(entry)              # 同步进记忆
    → await self._memory.maybe_summarize() # 达到阈值时折叠旧批次

Agent 规划: planner.plan_with_history(snapshot, goal, self._memory.context_entries())
    → serialize_history(context)           # 摘要条目 → {"summary": ...}，不占窗口
    → build_user_prompt(..., history)      # 摘要恒在头部
    → LLM
```

### 2.3 HistoryMemory 折叠策略（滚动摘要）

```text
window=5, batch=10（默认）
count - summarized_upto >= window + batch 时，把最旧的一批 batch 条折叠为一条摘要：
    batch = entries[summarized_upto : summarized_upto + batch]
    summary_lines.append(summarize(batch))
    summarized_upto += len(batch)

- 每条目只被摘要一次（增量，不重复计算）。
- 摘要器默认规则式；可注入 async callable（如 LLM 摘要）替换。
- context_entries() = [{"kind": "summary", "text": ...}] + entries[summarized_upto:][-window:]
```

### 2.4 序列化协议

Agent → Planner 边界（内存内条目）：

```python
# 摘要条目（kind="summary"），恒在列表头部，不占窗口
{"kind": "summary", "text": "click「登录」 成功\ninput「搜索框」 成功"}

# 普通条目（原样）
{"step": 12, "action": Action, "observation": Observation}
```

Planner → LLM 边界（模型视图，prompts 层转换）：

```json
[
  {"summary": "click「登录」 成功\ninput「搜索框」 成功"},
  {"action": "click", "target": "下载", "target_id": "e3",
   "success": true, "url": "https://example.com/result"}
]
```

### 2.5 安全边界

- 摘要只含 `action / target / 成功与否`，**不含**输入值（value）、URL、截图、堆栈 —— 与 `serialize_history` 白名单一致，避免敏感信息进入模型上下文。
- 原始条目 `entry` 对象被记忆与 `Agent.history` 共享，记忆只读不修改。

---

## 3. 分阶段实施计划

### 阶段 A：记忆核心

**涉及文件：**

- `agent-rpa/agent/core/memory.py`（新增）
- `agent-rpa/tests/test_memory.py`（新增）

**任务：**

- `summarize_entries()`：规则式摘要纯函数（条目 → 一行「动作 目标 → 结果」）。
- `HistoryMemory`：`add` / `maybe_summarize` / `context_entries` / `clear` / `count` / `summary` / `recent`。
- 折叠阈值与窗口边界（window + batch 触发，摘要不占窗口）。

**验收标准：**

- 15 条记录 + window=5/batch=10 → 摘要 1 条、窗口 5 条、原始 15 条完整保留。
- 摘要器可注入异步 callable，且只对折叠批次调用一次。

### 阶段 B：序列化协议升级

**涉及文件：**

- `agent-rpa/agent/prompts/planner.py`
- `agent-rpa/tests/test_prompt_serialization.py`

**任务：**

- `serialize_history()`：识别 `{"kind": "summary"}` 条目并渲染为 `{"summary": ...}`，摘要不占窗口。
- `build_user_prompt()`：摘要条目恒保留在头部，窗口只作用于常规条目。

**验收标准：**

- 含摘要的 history 输出为 `[{"summary": ...}, ...常规条目]`。
- 纯列表（无摘要）行为与 V0.3 完全一致（回归）。

### 阶段 C：Agent 接入

**涉及文件：**

- `agent-rpa/agent/core/agent.py`
- `agent-rpa/tests/test_memory.py`

**任务：**

- Agent 持有 `HistoryMemory`，`run()` 时 `clear()`。
- 三处记录点（自由模式 action / 步骤模式 wait+verify+action）同步 `memory.add()` + `maybe_summarize()`。
- `plan_with_history` / `plan_step` / `reflect` 改传 `context_entries()`。

**验收标准：**

- 长任务（> 15 步）中 Planner 收到的 history 含摘要条目，`Agent.history` 仍为完整原始列表。
- 全量回归通过：303（原） + 18（新增 Memory）= **321 个用例**。

### 阶段 D：文档收尾

**涉及文件：**

- `V0.5开发计划.md`（本文件）
- `AGENTS.md` / `README.md`

**任务：** 同步 V0.5 完成状态、目录结构、测试数。

---

## 4. 测试与验证策略

| 层级 | 验证内容 |
|---|---|
| summarize_entries | 条目格式化（Action/Observation 对象与 dict 兼容）、敏感字段不进入摘要 |
| HistoryMemory | 折叠阈值、增量只摘要一次、窗口边界、clear、自定义摘要器调用次数 |
| serialize_history | 摘要条目渲染、摘要不占窗口、纯列表回归 |
| build_user_prompt | 摘要恒在头部、窗口截断正确 |
| Agent 集成 | 长任务 Planner 收到摘要、history 原始完整、现有用例回归 |

最低回归命令：

```bash
cd agent-rpa
pytest -q
```

---

## 5. 交付清单

- [x] `agent/core/memory.py`：`summarize_entries` + `HistoryMemory`。
- [x] `serialize_history` / `build_user_prompt` 摘要协议。
- [x] Agent 记录与上下文传入接线。
- [x] `tests/test_memory.py` 单元 + 集成测试。
- [x] 全量回归通过（321 个用例）。
- [x] AGENTS.md / README 同步 V0.5 状态。
