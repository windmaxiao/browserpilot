# BrowserPilot 使用文档（V0.4）

> 基于 Playwright 的自主网页操作 Agent 框架。
> 本文面向**使用者**，描述当前版本（V0.4）如何安装、运行 Demo、使用规则引擎或 LLM Planner 编写自己的自动化任务。
> 架构与模块详解见 [AGENTS.md](AGENTS.md)，规划背景见 [Agentic_RPA_项目规划_V0.1.md](Agentic_RPA_项目规划_V0.1.md)。

## 一、环境要求

| 项目 | 要求 |
|------|------|
| Python | >= 3.10（当前开发环境为 3.11.9） |
| 浏览器 | Playwright Chromium（首次安装需下载） |
| 系统 | Windows / macOS / Linux |

> 注意：本机若同时存在多个 Python 版本，建议用 `py -3.11` 前缀执行命令，避免旧版 Python 干扰。

## 二、安装

```bash
cd agent-rpa

# 1. 安装依赖（含 dev 依赖，用于运行测试）
py -3.11 -m pip install -e ".[dev]"

# 2. 安装 Playwright 浏览器（Chromium）
py -3.11 -m playwright install chromium
```

## 三、运行 Demo

### 1. manual_demo.py — 手动模式（硬编码流程）

演示直接调用 `BrowserTool` 完成一段完整 RPA 流程：打开百度 → 自动检测页面版本（AI 版/经典版）→ 搜索"北京时间" → 获取第一条结果 → 点击（自动切换新标签页）→ 保存网页 HTML。

```bash
py -3.11 examples/manual_demo.py
```

- 浏览器为有头模式（弹出窗口），5 秒后自动关闭
- 结果保存到 `examples/结果/{时间戳}/正文.html`

### 2. agent_demo.py — 规则 Agent 模式（本地页面）

演示 `Agent + RuleBasedPlanner` 完整循环，使用本地搜索页 [search_page.html](examples/search_page.html) 保证流程确定性（不依赖外部网站）：

```bash
py -3.11 examples/agent_demo.py
```

预期 4 步完成：`goto(本地页) → input(北京时间) → click(搜索) → done`。

### 3. baidu_demo.py — 规则 Agent 跑真实网站

同一套规则引擎对真实百度执行搜索：

```bash
py -3.11 examples/baidu_demo.py
```

预期 4 步完成：`goto(百度) → input → click(百度一下) → done`。任务结束时终端会打印每一步的 action 与成功状态。

### 4. llm_agent_demo.py — LLM Agent 模式（本地页面）

演示 `Agent + LLMPlanner` 由大模型自主规划多步流程，仍使用本地搜索页 [search_page.html](examples/search_page.html) 保证流程确定性：

```bash
# 安装 openai 依赖
py -3.11 -m pip install -e ".[llm]"

# 方式一：OpenAI 或任意 OpenAI 兼容端点（中转/自部署）
$env:OPENAI_API_KEY = "sk-xxx"            # 必填；可选 OPENAI_BASE_URL / OPENAI_MODEL

# 方式二：国内大模型预设（DeepSeek/Kimi/智谱/通义/豆包/千帆/星火）
$env:LLM_PROVIDER = "deepseek"            # 预设名见下
$env:DEEPSEEK_API_KEY = "sk-xxx"

# 运行（可选参数：goal；环境变量 OPENAI_MODEL / OPENAI_BASE_URL 可覆盖默认模型与端点）
py -3.11 examples/llm_agent_demo.py "打开 <本地搜索页> 查找 北京时间"
```

**国内大模型预设**（`LLM_PROVIDER` 取值 → 厂商 / 默认模型 / Key 环境变量）：

| provider | 厂商 | 默认模型 | Key 环境变量 |
| :--- | :--- | :--- | :--- |
| `deepseek` | DeepSeek | deepseek-v4-flash | `DEEPSEEK_API_KEY` |
| `moonshot` | Moonshot Kimi | kimi-k3 | `MOONSHOT_API_KEY` |
| `zhipu` | 智谱 GLM | glm-4.7-flash | `ZHIPU_API_KEY` |
| `qwen` | 阿里云百炼 通义千问 | qwen3.8-max | `DASHSCOPE_API_KEY` |
| `doubao` | 火山方舟 豆包（推理接入点 ID） | doubao-seed-2-1-pro-260628 | `ARK_API_KEY` |
| `ernie` | 百度千帆 文心 | ernie-5.0 | `QIANFAN_API_KEY` |
| `spark` | 讯飞星火 | 4.0Ultra | `SPARK_API_KEY` |

Key 缺失时自动回退 `OPENAI_API_KEY`；预设的端点/模型均可被 `OPENAI_BASE_URL` / `OPENAI_MODEL` 覆盖。

**本地 .env 配置**（可选，免去每次设置环境变量）：写入 `.env` 文件即可，格式与上面一致：

```
# 当前目录 .env（项目级）
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-xxx
```

也可写到 `~/.browserpilot/.env`（用户级，跨项目通用）。加载顺序：先项目级后用户级；已存在的环境变量优先，不会被文件覆盖；文件缺失时行为与不配置完全一致（零依赖实现，无需安装 python-dotenv）。

- 模型只看到脱敏后的 Snapshot 摘要（不含 selector/HTML/Cookie/截图），只能输出项目定义的 Action
- 未配置 API Key 时程序给出明确提示并退出，不会发起任何请求
- 更多安全边界说明见 [agent-rpa/README.md](agent-rpa/README.md) 的 "V0.3 LLM 驱动 Agent Loop" 章节

**连通性测试**：配置 Key 后，可用脚本验证 7 家预设模型能否真实连通：

```bash
py -3.11 examples/check_llm_connectivity.py                  # 检查全部已配置 Key 的厂商
py -3.11 examples/check_llm_connectivity.py deepseek zhipu   # 只检查指定厂商
```

每家厂商各发起一次最小 JSON 请求，输出 `OK` / `FAIL`（含错误分类）/ `SKIP`（未配置 Key）；存在失败项时退出码为 1，适合接入 CI。脚本不打印任何 Key 或敏感信息。

### 5. llm_baidu_demo.py — LLM Agent 两阶段规划（真实百度）

演示 `Agent + TaskPlanner` 的**两阶段规划**（V0.4）：LLM 先把目标拆解为步骤队列，再逐步执行；等待/验收类步骤由框架直接执行（不经过 LLM）：

```bash
py -3.11 examples/llm_baidu_demo.py
# 可选自定义目标（参数省略时使用内置目标）
py -3.11 examples/llm_baidu_demo.py "打开 https://www.baidu.com 搜索 北京时间"
```

流程：`拆解(LLM 一次) → goto → input → click(搜索) → click(百科链接) → wait(框架直执行) → 队列耗尽即完成`。
点击 `target=_blank` 链接后自动跟随新标签页；目标中"关闭浏览器"由调用方负责，Agent 不会拆出该步骤。

与 `llm_agent_demo.py`（LLMPlanner 自由模式，LLM 自主决策直到输出 done）的对比：

| 维度 | LLMPlanner（自由模式） | TaskPlanner（两阶段） |
| :--- | :--- | :--- |
| 任务完成判定 | 依赖 LLM 输出 `done` | 步骤队列耗尽即完成 |
| 等待类步骤 | LLM 决策（易在已加载页面空转） | 框架直执行，不调 LLM |
| 可预测性 | 低，LLM 可能反复 wait | 高，步骤有清单 |
| 拆解失败 | — | 自动回退为自由模式 |

### 6. 运行测试

```bash
py -3.11 -m pytest          # 当前共 303 个用例，全部通过
```

## 四、Goal 语法（规则引擎理解的语言）

`RuleBasedPlanner` 通过 `parse_goal()` 从自然语言目标中提取要素：

| 写法 | 示例 | 提取结果 |
|------|------|----------|
| 打开 URL | `打开 https://www.baidu.com` | `url` |
| 搜索词 | `查找 北京时间` / `搜索 X` / `查询 X` | `search_keywords` |
| 点击目标 | `点击 北京时间 - 百度百科`（遇到下个动词截断）/ `点击"登录"` | `target_texts` |
| 等待加载 | `等待 页面加载完成` | `wait_loading` |
| 等待文本 | `等待 结果出现` | `wait_texts` |

规则驱动的流水线：

```
导航(未到达URL) → 输入搜索词 → 点击搜索按钮
  → (可选)等待条件满足 → (可选)点击目标 → done
```

**完成判定（V0.2 增强）：**
- **无显式点击目标**：已提交搜索且页面出现关键词即完成；纯导航任务到达 URL 即完成
- **有显式点击目标**：全部目标点击完成才算完成（不会因关键词出现提前收工）
- 文本匹配已归一化（忽略空格/破折号/下划线等分隔符差异）

**已知局限（V0.2 规则引擎）：**
- 搜索词暂只支持单个词；`点击 X` / `等待 X` 的 X 不能与动词连写（需空格或冒号分隔，如 `点击登录` 不识别，需写 `点击 登录`）
- 等待条件连续 10 次不满足会放弃（避免死循环）
- 更复杂的意图理解由 V0.3 的 **LLM Planner** 解决（见第三节 llm_agent_demo）

## 五、编写自己的规则 Agent 任务

```python
import asyncio

from agent.browser.playwright import BrowserManager
from agent.browser.snapshot import SnapshotGenerator
from agent.core.agent import Agent
from agent.core.executor import Executor
from agent.core.observer import Observer
from agent.core.planner import RuleBasedPlanner


async def main():
    goal = "打开 https://www.baidu.com 查找 北京时间"
    print("目标:", goal)

    manager = BrowserManager(headless=False)   # 有头模式调试
    await manager.start()
    try:
        tool = manager.create_tool()
        observer = Observer(SnapshotGenerator(manager.page))
        agent = Agent(
            observer=observer,
            planner=RuleBasedPlanner(),
            executor=Executor(tool),
            max_steps=20,
        )

        result = await agent.run(goal)

        if result.is_error:
            print("任务失败:", result.error)
        else:
            print("任务完成:", result.data)
            for step in agent.history:
                a, o = step["action"], step["observation"]
                print(f"  Step {step['step']}: {a.action} → 成功={o.success}")
    finally:
        await manager.stop()


asyncio.run(main())
```

### 自定义规则（add_rule）

内置规则覆盖通用流程；特殊站点可用 `add_rule` 注册自定义规则，**自定义规则优先于内置规则执行**：

```python
from agent.schema.action import Action

def my_condition(snapshot, goal):
    return "特殊网站" in snapshot.title

def my_action(snapshot):
    return Action(action="click", target="特殊按钮", params={"selector": "#special-btn"})

planner = RuleBasedPlanner()
planner.add_rule(my_condition, my_action)
```

规则签名：
- `condition(snapshot, goal) -> bool`：是否命中
- `action_fn(snapshot) -> Action | None`：返回动作；返回 `None` 则继续走内置规则

## 六、手动模式（不用 Agent，直接调工具）

```python
from agent.browser.playwright import BrowserManager, BrowserTool

manager = BrowserManager(headless=False)
await manager.start()
try:
    tool = BrowserTool(manager.page)
    await tool.goto("https://example.com")
    obs = await tool.click("button:has-text('登录')")
    print(obs.success, obs.url, obs.page_changed)
finally:
    await manager.stop()
```

`BrowserTool` 所有方法都返回 `Observation`（`success / url / title / page_changed / error / data`），不会向上抛异常。

### BrowserManager 常用配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `headless` | `False` | 无头模式（服务器部署用 `True`） |
| `slow_mo` | `50` | 操作间延迟（毫秒），模拟人类速度；`0` 关闭 |
| `channel` | — | 使用系统浏览器：`"chrome"`、`"msedge"` 等 |
| `executable_path` | — | 指定浏览器可执行文件路径 |
| `proxy` | — | 代理，如 `{"server": "http://proxy:8080"}` |

启动时自动应用反检测参数（隐藏 `navigator.webdriver` 等）。完整说明见 [agent-rpa/README.md](agent-rpa/README.md)。

## 七、常用 API 速查

| 组件 | 入口 | 作用 |
|------|------|------|
| `BrowserManager` | `agent/browser/playwright.py` | 浏览器生命周期（start/stop/page/新标签页跟随） |
| `BrowserTool` | 同上 | 11 种操作：goto/click/input/select/scroll/wait/download/upload/back/refresh/screenshot |
| `SnapshotGenerator` | `agent/browser/snapshot.py` | 页面 → 结构化 Snapshot（含 element_id、`:visible` 消歧选择器、页面关闭防御） |
| `Observer` | `agent/core/observer.py` | Agent 侧观察封装 |
| `RuleBasedPlanner` | `agent/core/planner.py` | 规则规划器（`parse_goal` + 8 条内置规则 + `add_rule`） |
| `LLMPlanner` | 同上 | LLM 自由模式规划器（自主决策直到输出 done） |
| `TaskPlanner` | 同上 | 两阶段规划器（`decompose` 拆解步骤队列 + `plan_step` 分步决策；wait/verify 框架直执行） |
| `TaskStep` / `TaskQueue` | 同上 | 任务步骤数据模型与队列 |
| `Executor` | `agent/core/executor.py` | Action → Playwright 调用翻译 |
| `Agent` | `agent/core/agent.py` | 主循环：`run(goal)` / `step(action)` / `observe()`；支持步骤/自由双模式 + 异常防护 |
| `setup_logging` | `agent/logging.py` | 控制台 + `logs/` 目录按天滚动文件日志 |

## 八、当前版本边界（V0.4 已完成，V0.5 待开始）

| 能力 | 状态 |
|------|------|
| 执行层（Browser Tool / Snapshot / Observation / Action Schema） | ✅ 完成 |
| 规则驱动 Planner（导航 / 搜索 / 点击目标 / 完成判定） | ✅ 完成 |
| 选择器定位唯一性（`:visible` + nth 消歧） | ✅ 完成 |
| click 页面变化检测（URL + 标题 + DOM 指纹） | ✅ 完成 |
| LLM Planner（LLMClient 抽象 + LLMPlanner + 安全序列化 + OpenAI Provider） | ✅ 完成 |
| 两阶段任务队列（TaskPlanner：拆解 → 分步执行，wait/verify 框架直执行） | ✅ 完成 |
| 浏览器关闭异常防护 / 停滞检测 | ✅ 完成 |
| 失败重试（机械 1 次 → Reflection 1 次 → 后退/刷新恢复 → 中止） | ✅ 完成（V0.4） |
| LLM 可重试错误指数退避自动重试 | ✅ 完成（V0.4） |
| 历史记忆（V0.5） | 📋 未开始 |
