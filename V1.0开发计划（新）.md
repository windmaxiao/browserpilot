# BrowserPilot V1.0 开发计划（修订版）

> **版本主题：** 生产长流程能力基座——iframe 自主操作与可重复验收
> **状态：** 🚧 进行中（子计划 A：iframe 框架层能力 ✅ 已完成）
> **前置版本：** V0.5 Memory（✅ 已完成）
> **版本判断：** V1.0 的核心是可靠的多层 iframe 操作能力和生产验收基座。登录、查询、下载、上传和 Excel 处理通过受控业务验收串联，不为单一 SAP 页面固化业务逻辑。

---

## 1. 目标与边界

V0.1～V0.5 已具备 Action、Snapshot、规则/LLM 规划、失败恢复和历史记忆。真实 SAP 报表流程暴露的关键缺口是：页面元素位于多层 iframe 内，框架既看不到也无法通过 `Page.locator()` 执行。

### 1.1 交付目标

1. ✅ Agent 在自由模式和步骤模式中均可发现、定位并操作多层嵌套 iframe 内控件，且主页面行为零回归（子计划 A 已完成）。
2. Browser 启动配置（B-1 主体 ✅ 已修复）、步骤等待失败（B-2）、手动调试 API（B-3）三项遗留健壮性问题闭环。
3. 建立本地可重复的多层 iframe 浏览器级测试（✅ 已建立：`test_snapshot_frame.py`），以及脱敏的真实长流程验收入口。
4. Provider 预设改为可维护的配置能力，而非将一次静态核对视为永久正确。

### 1.2 不包含

- 不实现跨任务持久化记忆或行为偏好学习。
- 不为 SAP 页面硬编码选择器、动态 ID 前缀或业务步骤；业务参数与 Excel 处理留在脚本层。
- 不将真实凭据、内网或外部系统流程纳入常规 CI。
- 不承诺复杂 Shadow DOM、验证码和人机验证自动化。

---

## 2. 子计划与依赖

| 子计划 | 主题 | 依赖 | 完成定义 |
|---|---|---|---|
| A | iframe 框架能力 | 无 | Snapshot 可见、Action 可达、重载可恢复、真实浏览器测试通过（✅ 已完成） |
| B | 基础健壮性收尾 | 无 | 待解决 #7/#8 具有明确且一致的 API 语义（原 #4 已随 BrowserManager 修复关闭） |
| C | Provider 预设维护 | 无 | 可覆盖、可诊断、可按官方信息更新 |
| D | 长流程验收基座 | A | 本地 fixture 自动验收加真实流程人工/夜间验收 |

> A、B、C 可独立推进；D 在 A 完成后接入真实 iframe 场景。资源有限时的优先级：B-1（✅ 已收尾）→ A（✅ 已完成）→ D → B-2/B-3 → C。

---

## 3. 子计划 A：iframe 框架层支持

### 3.1 定位契约

`selector` 只表示元素在**所属 document**中的选择器，不能编码 frame 信息。`ElementInfo` 新增内部字段：

```python
frame_path: tuple[str, ...] = ()
```

- 空 tuple 表示主页面，保持现有调用兼容。
- 每一段为父 document 内唯一定位 iframe 元素的 selector；执行时按顺序构建 `page.frame_locator(segment)`，最后调用 `.locator(selector)`。
- 不以 frame URL/name 作为唯一身份，不把 SAP 动态 ID 前缀写入框架通用逻辑。无法唯一定位的 iframe 必须消歧；仍不能消歧则跳过并记录日志。
- `frame_path` 与真实 selector 均不得暴露给 LLM；模型只能使用当前 Snapshot 的 `target_id`。可选暴露不敏感的 `frame_depth`。

### 3.2 快照与执行语义

1. `SnapshotGenerator` 递归遍历 iframe 元素及对应 Frame，提取每个 document 的按钮、输入框、链接、文本和下拉框；单次 Snapshot 内 `element_id` 全局唯一。
2. 进入子 Frame 前使用 `frame.wait_for_load_state` 和有限超时；超时只跳过该 Frame，不使整次 Snapshot 失败。
3. `Executor` 的映射变为 `element_id → (selector, frame_path)`；路径仅能从当前 Snapshot 注入，禁止 Action 参数伪造。
4. `BrowserTool` 为 click/input/select/download/upload 提供内部 Locator 解析；主页面仍使用 `page.locator(selector)`。
5. iframe 操作以目标 Frame 的轻量 DOM 指纹、URL/标题变化及执行后 Snapshot 判定变化；不对所有 Frame 做全量指纹。
6. Frame 导航、分离或 locator 失效后，旧路径立即作废；对 iframe 目标机械重试前必须重新 Observe 并重新解析 `target_id`。

**可点击文本识别（增强）：** `p/span/div/li/td/label` 带交互属性（`onclick`/`gcode`/`data-source`/`data-id`/`data-code`/`data-action`、`role=link|button`）或 `cursor:pointer`（排除 `div`/`td` 布局容器）的文本元素并入 `buttons` 交互元素，每帧上限 50；解决 ERP 系统 `div/span/p` 充当点击入口、Snapshot 只见文本而 LLM 无法点击的问题（ydgx 应用中心入口）。

### 3.3 分期与验收

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| A-0 原型 | 本地三层 iframe fixture 验证路径模型 | Snapshot 得到正确层级和唯一 target_id |
| A-1 快照可见 | 数据模型、递归提取、超时和消歧 | 主/子 Frame 元素均可见，主页面快照回归不变 |
| A-2 执行可达 | Locator 解析、Executor 透传与 Action 执行 | 可在嵌套 Frame click/input/select/download/upload |
| A-3 失效恢复 | Frame 重载、变化判定、重新观察后重试 | 不复用旧定位，失败可诊断 |
| A-4 回归与安全 | 序列化与浏览器级测试 | 模型上下文不含 selector/frame_path；全量测试通过 |

### 3.4 测试要求

- 使用真实 Playwright 浏览器和本地 fixture，覆盖三层 iframe、重复 iframe、延迟加载、frame 重建、主页面回归与跨源 Frame（本地双端口）。
- 补充：跨源 Frame（本地双端口）测试为「可选增强」——需先在目标环境确认 Playwright `frame_locator` 对跨源 Frame 的可用性（个别代理/证书场景存在坑）；若验证成本高，可先以同源主场景为主，跨源留作后续。
- Mock 测试只验证参数传递，不能替代浏览器级测试。
- 保留 Snapshot/Executor 定位契约测试；新 Snapshot 后不得复用旧 `target_id`。

---

## 4. 子计划 B：基础健壮性收尾

### B-1 启动与 Context 配置（原待解决 #4，✅ 已收尾）

`user_data_dir` 不是 `chromium.launch()` 合法参数，必须使用 `launch_persistent_context()`；因此不得以"全量透传 launch kwargs"解决此问题。

- ✅ 已修复：`BrowserManager.start()` 不再静默丢弃合法 launch 参数（`user_data_dir` / `env` / `devtools` / `IgnoreDefaultArgs` 等原样透传），见 [playwright.py](agent-rpa/agent/browser/playwright.py)；
- ⏳ 剩余：`launch_options` / `context_options` 拆分与持久化上下文分支（`launch_persistent_context`）、错位参数启动前显式报错、`stop()` 按分支清理——作为后续增强，不再绑定 `待解决问题.md` 编号。

### B-2 步骤模式 wait 失败（待解决 #7）

- 区分临时可重试错误与页面/浏览器已关闭等不可恢复错误。
- 临时失败只进行一次有限重试；仅在 Page 可用时允许 back/refresh 恢复。会话关闭必须返回明确失败原因。
- 验收：临时 wait 失败可恢复；页面关闭不触发错误恢复或无限等待。

### B-3 手动 API 防护（待解决 #8）

- 保持 `observe()` / `step()` 调试语义清晰，不以空 Snapshot 掩盖异常。
- 新增或明确 `safe_observe()` / `safe_step()`：安全入口返回可辨识失败，原始调试 API 可保留异常。
- 验收：浏览器关闭时安全入口不崩溃，调用方能区分失败与真实空页面。

---

## 5. 子计划 C：Provider 预设维护

1. 显式 `model` / `base_url` 始终优先，用户可绕过预设。
2. 依据厂商官方文档更新默认值；支持 OpenAI 兼容 `/models` 的厂商可提供**可选**模型发现/诊断，网络查询不应成为构造函数必经路径。
3. Provider 元数据至少记录端点、默认模型、环境变量和 JSON 模式支持情况；错误提示说明模型名、账号权限或端点可能已变更，但不得泄露密钥或完整请求。
4. 单元测试验证本地预设、覆盖优先级与错误文案；实际厂商可用性检查作为独立人工/定期任务。

---

## 6. 子计划 D：长流程验收基座

| 层级 | 环境 | 用途 | 常规 CI |
|---|---|---|---|
| 框架验收 | 本地 fixture + Playwright | iframe、下载/上传、重载、结果文件断言 | 是 |
| 业务验收 | 脱敏 SAP/控制中心环境 | 登录、查询、真实报表下载和 Excel 产物 | 否；手工或受控夜间任务 |

`examples/ex_robot/ydgx/ydgxdjsx_agent.py` 已迁移至 BrowserPilot 框架（`BrowserManager` + `BrowserTool`），以「LLM 局部辅助」方式运行：**登录、应用中心跳转、旧 SAP 跳转、菜单导航**由 LLM Agent 驱动（账号密码以占位符本地注入、跳转失败自动回退确定性 XPath），报表下载等确定性步骤保持手动模式；它是业务流程说明和人工验收基线，不是 BrowserPilot 的直接回归用例。

- 先建立 BrowserPilot 原生的本地流程，断言下载文件存在/格式正确、上传动作已触发与关键页面状态。
- 再在 SAP 环境验证原有硬编码路径是否可由框架 `target_id` 路由替代，业务脚本继续负责业务参数、凭据和 Excel 处理。
- 凭据只能从受控环境注入；日志、测试产物和报告不得包含账号、Cookie、完整 URL 查询参数或生产数据。

---

## 7. 实施顺序与发布门槛

1. ✅ 完成 B-1，校正 BrowserManager 的配置 API 语义（launch 参数透传已修复；持久化上下文拆分作为后续增强）。
2. ✅ 完成 A-0～A-4 与浏览器级 fixture 测试（`test_snapshot_frame.py`，真实 Chromium 三层 iframe）。
3. 完成 D 的框架验收后，再接入受控业务验收。
4. 完成 B-2、B-3；C 作为独立维护项，在发布前核对一次。

V1.0 发布必须同时满足：

- `cd agent-rpa && pytest -q` 全绿；
- 本地真实浏览器的多层 iframe fixture 覆盖发现、操作、frame 重建与下载/上传；
- 主页面 Snapshot/Executor 行为无回归；
- 模型上下文未泄露 selector、frame_path、凭据或生产数据；
- 至少一次脱敏业务流程验收有可追溯结果和人工确认。

---

## 8. 交付清单

- [x] iframe 数据模型、递归 Snapshot、帧内 Locator、重载恢复与测试（子计划 A，✅ 已完成）。
- [x] BrowserManager 启动参数透传修正（B-1 主体，✅ 已完成；持久化上下文拆分 ⏳ 后续增强）。
- [ ] 步骤 wait 失败分类与安全手动 API（B-2 / B-3）。
- [ ] Provider 可维护预设、覆盖与诊断文案（C）。
- [ ] 本地多层 iframe fixture（✅ 已有）、下载/上传验收与业务验收说明（D 进行中）。
- [ ] 同步更新 AGENTS.md、README 与待解决问题状态（✅ 本轮已同步）。
