# BrowserPilot V1.1 开发计划

> **版本主题：** 稳定化与生产可观测——清偿已知问题，补齐降级与追踪能力；同步预研生产化能力增强（S7）
> **状态：** 🚧 S1/S2/S3 已完成（2026-09-02），S4 进行中
> **前置版本：** V1.0（🚧 子计划 A ✅、B-1 ✅；B-2/B-3、C、D 未完成，处置见第 1.2 节）
> **问题基线：** [待解决问题.md](待解决问题.md) 2026-08-31 快照——52 项（🔴 1 | 🟡 14 | 🟢 37 | ⏸️ 1）
>
> **版本判断：** V0.1~V0.5 与 V1.0 子计划 A 已让框架「能看、能想、能做、能恢复、能记忆」。当前制约不是缺新功能，而是已知缺陷面（52 项）与生产化短板（无降级、无追踪、脱敏漏洞）。V1.1 以问题清偿为主线（S1~S6），按「正确性 → 稳定性 → 安全 → 可观测 → 工程化」顺序收口；在此之上以独立子计划 S7 补齐四项低成本、高价值的生产化能力（登录态复用、安全护栏、LLM 成本优化、执行报告），并给出 V1.2+ 演进路线图。S7 不阻塞主线发布，可后置为 V1.1.x 小版本。

---

## 1. 目标与边界

### 1.1 交付目标

1. 🔴 高优先清零（#4 download 文件名路径穿越）。
2. 🟡 中优先全部闭环（含 2026-08-31 增补的弹窗处理 #50、规划降级 #51）。
3. 🟢 低优先消减 ≥ 80%（确认保留/关闭的条目除外）。
4. 新增四项生产化能力：JS 弹窗应对、规划失败降级、日志脱敏收口、run 级追踪与指标。
5. 长流程验收（承接 V1.0 子计划 D）至少完成框架级验收一轮。
6. （S7，可后置）新增四项能力增强：登录态与会话管理、安全护栏、LLM 成本优化、执行报告可视化。

### 1.2 与 V1.0 未竟事项的衔接

| V1.0 遗留 | 处置 |
|---|---|
| 子计划 C（Provider 预设维护） | 并入本计划 S6（对应 #17），发布前按官方清单人工核对一次 |
| 子计划 D（长流程验收基座） | 作为本计划贯穿验收主线，见第 9 节 |
| B-2 / B-3（原待解决 #7/#8，旧编号） | 原编号已随 2026-08-27 清单重排失效；其诉求（wait 失败分类、手动 API 防护）由 #15/#41 等新编号条目部分覆盖，S1 实施时先复核剩余缺口再决定是否补条目 |

### 1.3 不包含（V1.2+ 演进，路线图见第 10 节）

- #52 Shadow DOM / Canvas / 多模态视觉通道（需架构级投入）。
- #53 跨任务持久化记忆与 RAG 检索。
- #24 定位意图与 Playwright 方言分离（执行器可替换的中期重构）。
- 平台化三件套（任务模板持久化 + 定时调度 + 失败告警）、录制回放完整版、extract 数据提取动作（新 Action 类型）。
- #5 texts 含 span 噪音（⏸️ 维持暂不处理）。
- 不新增 Action 类型（extract 等留待 V1.2）、不做多浏览器/移动端适配。

---

## 2. 问题总览与子计划映射

52 项问题的归属全景（编号均为 [待解决问题.md](待解决问题.md) 当前编号）：

| 子计划 | 主题 | 条目数 | 编号 |
|---|---|---|---|
| S1 | 正确性修复 | 12 | #9 #10 #12 #13 #14 #15 #16 #19 #20 #39 #41 #49 |
| S2 | 安全与执行契约 | 6 | #4 #28 #30 #40 #42 #43 |
| S3 | 稳定性与降级韧性 | 6 | #6 #7 #25 #37 #50 #51 |
| S4 | 安全与脱敏 | 7 | #11 #21 #34 #45 #46 #47 #48 |
| S5 | 可观测性与性能 | 4 | #22 #44 #54 #55 |
| S6 | 工程化与测试卫生 | 13 | #8 #17 #18 #23 #26 #27 #29 #31 #32 #33 #35 #36 #38 |
| S7 | 生产化能力增强（新能力，无问题编号） | 4 | S7-1 ~ S7-4 |
| 展望 | V1.2+ 能力演进 | 3+ | #24 #52 #53 + 路线图新增项（第 10 节） |
| 不纳入 | 暂不处理 | 1 | #5 |

---

## 3. 子计划 S1：正确性修复

目标：消除「静默出错 / 误判 / 状态污染」类缺陷。全部可本地 pytest 验证，先行实施。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| #9 | `llm_retries=0` 静默禁用规划 | 构造校验 `>= 1` 或 `max(1, llm_retries)` |
| #10 | wait 浮点字符串静默回退 1000ms | 先 float 后 int，失败记 warning |
| #12 | 重复动作指纹不含 value/params，误杀增量操作且跨批不清零 | 指纹纳入语义参数；批耗尽复位计数 |
| #13 | 页面恢复成功后停滞状态变量未复位 | 恢复成功与断批处统一复位四变量 |
| #14 | parse_goal 全局引号并入点击目标，完成判定不可达 | 引号兜底限定在 CLICK_RE 窗口内 |
| #15 | Planner 协程四处裸 await，异常击穿主循环 | 统一套 `_safe_*` 防护，异常视为「本轮无法规划」 |
| #16 | 未识别 `finish_reason=length`，截断响应当正常输出 | 检查并映射为不可重试错误，消息注明「截断」 |
| #19 | 重复动作被跳过未反馈 Planner | 跳过分支同样 `_notify_planner_result` |
| #20 | 非规则产生的 input 失败误清 `_searched` | 回滚前校验 `_last_intent == ("input",)` |
| #39 | click 已生效后置采样异常误报失败，不可逆操作可能重复执行 | 采样独立 try；点击成功即成功；Executor 重试前二次确认 |
| #41 | 步骤模式 wait/verify 失败重试同一步重复入史 | 最终判定后统一记录一次 |
| #49 | 点击目标解析漏半角标点 | 分隔集补 `,;.`；归一化剥尾部标点 |

**验收：** 每条先补复现测试再修复；`pytest -q` 全绿；#39 需新增「不可逆动作不重复执行」回归用例。

---

## 4. 子计划 S2：安全与执行契约

目标：收口 M5 之后仍存在的信任边界缺口与接口设计缺陷。含唯一 🔴。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| **#4 🔴** | download 直接拼接 `suggested_filename` 落盘，存在路径穿越面 | 取 `(Path(name)).name` 并拒绝空名；文件名清洗 + 恶意名告警日志 |
| #28 | Executor 校验失败 Observation 缺 url | 失败路径统一附 `url`（try 包裹） |
| #30 | 批量规划 upload 越权被静默丢弃 | 记录告警日志（批量语义保持不中断） |
| #40 | Action 无法携带 frame_path，selector 直通模式跨帧必错配 | 支持 `params["frame_path"]` 显式传入并配套校验；文档明确边界 |
| #42 | `ok()/fail()` 意外 kwargs 静默并入 data；fail 缺 url/title 形参 | 改显式关键字参数；fail 与 ok 形参对齐 |
| #43 | `parse_action_list` 批量数量无硬校验 | 超限抛不可重试错误走修复回路，或截断 + 告警 |

**验收：** #4 以恶意文件名（`../`、绝对路径、空名）回归测试收口；#40 补 iframe 下手动构造 Action 的用例。

---

## 5. 子计划 S3：稳定性与降级韧性（✅ 已完成，2026-09-02）

目标：让 Agent 在阻断场景下「有策略、能降级、不泄漏」。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| #6 | iframe 递归遍历无帧数/深度/总时间上限；`_extract_scope` evaluate 无超时兜底 | `max_frames` + 总时间预算参数；evaluate 包 `asyncio.wait_for` |
| #7 | `subscribe_page` 只增不减，监听器与旧 Generator 泄漏 | 提供 `unsubscribe_page`；业务示例运行结束注销 |
| #25 | `timeout_seconds` 取消可能落在 Playwright 调用中途 | 超时后主动 refresh 复位并在文档注明 |
| #37 | `_recover_page` 固定 sleep 0.5s 硬编码 | 提取为可配置常量 |
| #50 | JS 弹窗无处理、验证码无策略 | 注册 `page.on("dialog")` + 可配置接受/拒绝策略并记入 Observation；Snapshot 填充 `dialogs`；验证码预留「暂停 + 人工接管回调」扩展点 |
| #51 | LLM 规划失败无规则引擎回退与人工确认通道 | Agent 支持 `fallback_planner`（连续失败 N 步切换）；预留 human-in-the-loop 暂停/恢复接口 |

**验收：** alert/confirm 页面 fixture 验证弹窗策略生效且 Observation 可读；LLM 连续失败场景验证可降级到注入的兜底 Planner 而非直接中止；深帧大页面 Observe 耗时有上界。

---

## 6. 子计划 S4：安全与脱敏

目标：兑现「模型上下文与日志不含敏感信息」的既定承诺，堵住五处泄露面。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| #11 | `sanitize_url` 全等匹配漏变体；未剥 `userinfo@` | 改词根/正则匹配；掩码前剥离凭据段（提示词与 debug 日志两处同源修复） |
| #21 | `OpenAILLMClient` 构造期向全局 env 注入任意 `.env` 键 | 收敛为显式调用或键白名单 |
| #34 | `_PROJECT_ENV` 依赖固定目录结构 | 正式分发前复核发现策略，先在代码注释与文档标注限制 |
| #45 | LLM 原始响应全文 info 级落盘 | 降为 debug 或仅失败时记录；常规路径只记解析摘要 |
| #46 | Executor 日志原样记录 input value 明文 | 日志层统一掩码（如 `***4位`） |
| #47 | `observe_simplified()` 未脱敏且为死代码 | 删除该方法 |
| #48 | `_map_error` 内插 SDK 异常原文，可能回显请求体 | 仅保留类型名 + 结构化错误码 |

**验收：** 串一遍含密码输入与带 token URL 的流程，断言 logs/ 产物与 LLM 提示词中无明文敏感值（新增专项脱敏测试）。

---

## 7. 子计划 S5：可观测性与性能

目标：生产环境「看得见每一步、算得清每一分钱」。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| #22 | `setup_logging` 全局 `remove()` + CWD 相对目录 | 文档明示单次调用；日志目录支持环境变量覆盖、默认绝对路径化 |
| #44 | `_walk_scopes` 宽泛 except 静默吞异常 | 收窄预期异常；其余按 frame path 记 warning |
| #54 | 无 run_id/step_id 贯穿、无结构化 trace 与汇总指标 | run 生成 `run_id` 并贯穿日志；每步产出结构化事件（规划/执行耗时、重试次数）；run 结束输出成功率与失败分布汇总；可选 OpenTelemetry 埋点 |
| #55 | 每轮至少一次 LLM 调用，延迟成本线性增长 | 成功轨迹录制为确定性脚本，复跑回放、失败回退 LLM（依赖 #51 的降级骨架）；以 #54 指标量化收益 |

**验收：** 一次 demo run 产出含 run_id 的结构化事件文件与末尾汇总（步数/重试/成功率/耗时分布）；LLM 调用次数在表单场景 ≤ 批量规划已有基线。

---

## 8. 子计划 S6：工程化与测试卫生

目标：低风险清理，随各阶段穿插进行。

| 编号 | 问题 | 修复要点 |
|---|---|---|
| #8 | 核心依赖无版本上限 | `playwright>=1.40,<2` 等加上限 |
| #17 | Provider 预设模型名需人工核对（承接 V1.0 子计划 C） | 发布前对照官方清单核对一次；错误文案引导「模型名可能已变更」 |
| #18 | iframe 混合标签 / 超时跳过无测试 | 补 `<frameset>`/`<object>` fixture 与永不加载帧用例 |
| #23 | Mock 回退路径与批量 JS 路径差异 | 关键语义用例挂真实浏览器；Mock 用例标注差异范围 |
| #26 | 业务示例依赖未声明 | 增加 `examples` extra 或示例目录 requirements |
| #27 | `element_id` 文档 e0 与实际 e1 不符 | 统一三处文档为 `e1, e2, ...` |
| #29 | `Optional[list]` 注解不精确 | 改 `Optional[Sequence[str \| Path]]` |
| #31 | pyproject 版本号 0.1.0 过时 | 更新为 1.1.0（随本版本发布） |
| #32 | snapshot.py 过长（810 行） | 拆分提取 JS / CSS 转义 / frame 遍历模块 |
| #33 | `agent/tools/` 空模块 | V1.1 内若 #51/#53 需要「可注入工具」载体则启用，否则移除 |
| #35 | `attributes` 类型标注与 None 传参不符 | 标注 `dict \| None` |
| #36 | `plan_step` 变量名与参数语义重叠 | 局部重命名 |
| #38 | HistoryMemory 构造校验属防御性冗余 | 确认保留即可（本子计划内直接关闭） |

---

## 9. 子计划 S7：生产化能力增强（新能力，可后置为 V1.1.x）

目标：在不冲击问题清偿主线的前提下，以低成本补齐四项高价值生产化能力。全部为新增能力开发（无对应问题编号），独立验收、独立发布门槛，允许后置为 V1.1.x 小版本；排期不足时可整体顺延至 V1.2（顺延项与第 10 节路线图合并）。

| 项 | 能力 | 要点 | 与既有条目的衔接 |
|---|---|---|---|
| S7-1 | 登录态与会话管理 | `BrowserManager` 支持 `storage_state` 导出/加载：登录态一次人工/半自动获取、后续任务静默复用；会话失效检测 + 重新登录钩子 | 验证码「暂停 + 人工接管」复用 #50 扩展点；登录类长流程验收的前置减负项 |
| S7-2 | 安全护栏 | ① 域名白名单：`goto`/点击跳转前校验目标域，越界拒绝并记审计；② 敏感操作二次确认：提交/删除/支付类按钮执行前回调确认（策略可配置：放行/确认/拒绝）；③ run 级动作审计日志（何时、哪个动作、目标与结果） | 确认回调复用 #51 human-in-the-loop 接口；审计事件复用 #54 结构化事件通道；与 S2/S4 的信任边界思路同源 |
| S7-3 | LLM 成本优化 | ① Snapshot 增量序列化：与上一轮对比仅输出新增/消失/变化元素（长任务 token 线性下降），保持脱敏与截断语义；② 大小模型分流：规划/Reflection 用大模型、批量动作决策用小模型（`LLMClient` 按调用角色传 model 配置）；③ 步骤模式批量：TaskPlanner 同页面连续多字段合并为一次 LLM（消除逐字段调用，见下方诊断记录） | 以 #54 指标量化收益（token/调用次数前后对比）；增量序列化需等价性测试（增量视图与全量视图解析结果一致）；③ 复用自由模式 `parse_action_list` 批量协议 |
| S7-4 | 执行报告可视化 | run 结束生成 HTML 报告：步骤时间线 + 每步截图（screenshot 已有 base64 能力）+ Snapshot/Action/Observation 详情 + 失败点高亮；报告经脱敏过滤（复用 S4 规则） | #54 结构化事件 + #55 轨迹数据的呈现层；报告文件落盘 `reports/`（与 logs/ 同级策略） |

> **诊断记录（2026-09-02，实测慢 + 耗 token，根因分析）：**
> **① 慢**：动作数 ≈ LLM 调用次数——自由模式批量循环中任一动作 `page_changed` 即 `break` 断批（[agent.py](agent-rpa/agent/core/agent.py)），导航/跳转类任务退化为每动作 1 次调用；**步骤模式 `TaskPlanner.plan_step` 是逐字段一次 LLM，不享受批量**（表单填 N 字段调 N 次，`llm_baidu_demo.py` 即此路径）。总耗时 = 动作数 ×（网络 + 推理延迟）串行累加。
> **② 耗 token**：每次 prompt 全量序列化整页 Snapshot + 历史窗口（[prompts/planner.py](agent-rpa/agent/prompts/planner.py)）；失败路径额外调用——动作失败 `reflect()` 1 次、解析失败修复 1 次、可重试错误指数退避重发。
> 已固化为两项后续优化：**S7-3③ 步骤模式批量**（同页字段 N→1）与 **S7-3① 增量序列化**（每轮 token 长任务线性下降）。可选加速先决：S5 #54 指标量化每轮 prompt 字符与调用次数、#55 轨迹回放复跑免 LLM。**

**验收：**
- S7-1：登录态复用 demo——同一站点两次运行，第二次无登录操作直达业务页。
- S7-2：白名单外域名被拒绝且有审计记录；敏感按钮在「确认」策略下暂停并可通过回调放行/取消。
- S7-3：长任务（≥ 15 步）增量序列化后 prompt token 较全量基线下降 ≥ 50%，且规划行为无回归。
- S7-4：报告可在浏览器直接打开，含全部步骤、截图与失败高亮；含密码流程的报告无明文敏感值。

---

## 10. V1.2+ 演进路线图

> 来源：2026-08-31 全项目代码审查后的功能演进建议 + 原有展望条目。按「先平台化交付价值、再降本增效」排序。

| 优先 | 方向 | 内容 | 来源 |
|---|---|---|---|
| 1 | 平台化三件套 | 任务模板持久化（JSON/YAML：goal + 变量 + 调度规则）、定时调度器（cron 语义）、失败告警（webhook/邮件）——从一次性 CLI 走向「定义一次、定时反复执行」的 RPA 生产形态（`Agent.run(goal)` 接口已为此备好） | 新增 |
| 2 | 执行报告（若 S7-4 顺延） | 同 S7-4，随平台化一并交付 | S7-4 顺延 |
| 3 | 登录态管理（若 S7-1 顺延） | 同 S7-1，平台化的前置依赖 | S7-1 顺延 |
| 4 | 录制回放完整版 | 人工操作录制为确定性脚本（selector/frame_path 基础设施已齐备）→ 确定性回放 → 失败 LLM 修复；LLM token 成本降一个数量级（V1.1 #55 只做框架级底座） | #55 延伸 |
| 5 | extract 数据提取动作 | 新增 Action 类型：按 Snapshot 元素批量提取表格/列表 → JSON/CSV/Excel 输出；RPA 最高频需求（报表搬运），承接 V1.0 子计划 D 深化 | 新增 |
| 6 | 视觉兜底 | 启用 `ElementInfo.bbox`（V0.2 遗留字段）+ 截图元素框选标注 + 多模态 LLM，覆盖 DOM 语义提取失效场景：canvas 报表、Shadow DOM、自绘组件 | #52 |
| 7 | 跨任务经验记忆 | 成功任务步骤序列存为「经验库」，相似任务优先复用（URL → 已验证脚本），复用失败降级 LLM 自主规划；与录制回放构成「越用越快」正循环；AGENTS.md V0.5 备注已预留此方向 | #53 |
| 8 | 执行器可插拔 | `BrowserTool` 正式抽为 Protocol/ABC，做第一个非 Playwright 实现验证抽象成立——不被第二个实现验证过的抽象都是假的 | #24 |
| 9 | LLM 成本优化（若 S7-3 顺延） | 同 S7-3；并评估批量规划规模（当前 10 个/批）的进一步扩大 | S7-3 顺延 |

---

## 11. 实施顺序与发布门槛

```
阶段一（正确性）   S1 + S2      → 纯本地可测，问题面从 52 项降至 ≤ 34
阶段二（稳定性）   S3 + S4      → 弹窗/降级/脱敏三项能力落地
阶段三（生产化）   S5 + S6      → 追踪指标与工程化收尾
阶段四（能力增强） S7           → 独立发布门槛，可后置为 V1.1.x 小版本
贯穿               长流程验收   → 每阶段结束跑一轮框架级验收防回归
```

V1.1 主版本发布必须同时满足：

- `cd agent-rpa && pytest -q` 全绿；
- 🔴 清零、🟡 清零、🟢 消减 ≥ 80%（#5 维持 ⏸️、#38 确认关闭计入完成）；
- 弹窗、降级、脱敏、追踪四项新能力各有自动化测试守护；
- 含密码/token 的端到端流程日志产物经脱敏断言；
- 长流程框架级验收（本地 fixture：登录 → 查询 → 下载 → 断言产物）通过一次并留存结果；
- AGENTS.md、README、使用文档与待解决问题状态同步更新。

V1.1.x（S7）发布门槛（若随主版本交付则并入上方检查）：

- S7 各项各有自动化测试守护（含 S7-3 token 收益量化、S7-4 报告脱敏断言）；
- 登录态复用、白名单拦截、增量序列化等价性三项 demo/用例留存结果。

---

## 12. 交付清单

- [x] S1 正确性修复 12 项（含复现测试先行）— 2026-09-02 全部完成（#9/#10/#12/#13/#14/#15/#16/#19/#20/#39/#41/#49）。
- [x] S2 安全与执行契约 6 项（🔴 #4 清零）— 2026-09-02 全部完成（#4/#28/#30/#40/#42/#43）。
- [x] S3 弹窗应对 + 降级/人工确认扩展点 + 资源泄漏与预算收口 — 2026-09-02 全部完成（#6/#7/#25/#37/#50/#51）。
- [ ] S4 五处脱敏泄露面收口 + 专项脱敏测试。
- [ ] S5 run_id 贯穿 + 结构化事件 + 汇总指标；轨迹回放（可延后至 V1.2，若排期不足）。
- [ ] S6 工程化 13 项（含 #17 Provider 核对、#32 模块拆分）。
- [ ] S7 生产化能力增强 4 项（S7-1 登录态 / S7-2 安全护栏 / S7-3 LLM 成本优化 / S7-4 执行报告；可整体后置为 V1.1.x）。
- [ ] 长流程框架级验收一轮 + 文档同步。
